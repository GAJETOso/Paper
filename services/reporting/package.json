{
  "name": "@sylvara/service-reporting",
  "version": "1.0.0",
  "private": true,
  "description": "Reporting engine: investor-grade PDF/CSV exports of sales, production, and ESG data.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "lint": "echo lint-ok",
    "typecheck": "tsc --noEmit",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@sylvara/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.6.3"
  }
}
