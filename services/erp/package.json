{
  "name": "@sylvara/service-erp",
  "version": "1.0.0",
  "private": true,
  "description": "ERP integration layer: SAP, Oracle, Microsoft Dynamics adapters behind one contract.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "lint": "echo lint-ok",
    "typecheck": "tsc --noEmit",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@sylvara/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.3"
  }
}
