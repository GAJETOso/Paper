# Development image: full toolchain, hot reload, mounted source.
FROM node:22-alpine
RUN corepack enable && apk add --no-cache git
WORKDIR /repo
EXPOSE 3000 4100 4200 4300 4400 4430
CMD ["sh", "-c", "pnpm install && pnpm dev"]
