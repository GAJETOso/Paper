{
  "name": "@sylvara/customer-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Customer portal: orders, invoices, order tracking, per-account sustainability reports.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "lint": "echo lint-ok",
    "typecheck": "tsc --noEmit",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@sylvara/shared": "workspace:*",
    "@sylvara/auth": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3"
  }
}
