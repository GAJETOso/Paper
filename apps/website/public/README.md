# Static assets

Files placed here are served as-is at the site root (e.g. `public/og-image.png` → `/og-image.png`).

Favicon, manifest, robots.txt, and sitemap.xml are generated dynamically from `src/app/` and do not need files here — see `icon.svg`, `manifest.ts`, `robots.ts`, `sitemap.ts`.
