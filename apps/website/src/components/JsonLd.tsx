/** Renders a JSON-LD structured-data script tag (Organization, Product, FAQ…). */
export function JsonLd({ data }: { data: object }) {
  return (
    <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }} />
  );
}
