{
  "name": "@sylvara/whatsapp-bot",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp Business Cloud API bot: catalog, quotations, order tracking, AI assistant, human handover.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "build": "tsc",
    "lint": "echo lint-ok",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/router.test.ts"
  },
  "dependencies": {
    "@sylvara/ai": "workspace:*",
    "@sylvara/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.3"
  }
}
