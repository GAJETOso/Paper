{
  "name": "@sylvara/supplier-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Supplier portal: RFQs, purchase orders, invoicing, compliance documents, sustainability scorecards.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "lint": "echo lint-ok",
    "typecheck": "tsc --noEmit",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@sylvara/shared": "workspace:*",
    "@sylvara/auth": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.3"
  }
}
