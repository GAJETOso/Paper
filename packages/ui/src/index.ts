/**
 * @sylvara/ui — shared UI primitives.
 *
 * The flagship website keeps page-level components local (apps/website/src/
 * components) for now; primitives promoted here must stay dependency-free
 * apart from React so every portal can consume them.
 */
export { Button } from "./Button";
export { Badge } from "./Badge";
